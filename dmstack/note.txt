package main

import (
	"fmt"
	"time"
)

func say(ch chan int, s string) {
	for i := 0; i < 5; i++ {
		time.Sleep(100 * time.Millisecond)
		fmt.Println(s)
	}
	ch <- 1
} 

func main() {
	ch:= make(chan int)
	go say(ch, "world")
	//say("hello")
	//<- ch
}

